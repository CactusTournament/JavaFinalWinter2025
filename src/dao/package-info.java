/**
 * Data Access Objects (DAO) package.
 *
 * <p>Contains classes responsible for CRUD operations against the
 * application's database tables. Each DAO maps database rows to the
 * corresponding model objects and exposes simple create/read/update/delete
 * methods used by services and the application entry point.</p>
 *
 * @since 2025-12-06
 */
package dao;
